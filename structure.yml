GLOBAL:
  prompts:
    - prompt: app_name

express:
  prompts:
    - prompt: install logging utility (chalk)
      prompt: support enviornment variables (dotenv)
  options:
    - cors
    - body-parser
    - axios
    
react:
  options:
    - styled-components -default
    - react-router-dom - default
    - easy-peasy
    - axios
    - formik
    - yup

simple:
  prompts:
    - prompt: language (js/ts)